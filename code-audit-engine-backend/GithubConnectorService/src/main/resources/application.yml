spring:
  application:
    name: GithubConnectorService

server:
  port: ${SERVER_PORT:8082}

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health

logging:
  level:
    root: ${LOG_LEVEL:INFO}
