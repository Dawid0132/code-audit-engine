spring:
  application:
    name: CodeAuditEngineClient

server:
  port: ${SERVER_PORT:8090}

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: ${LOG_LEVEL:INFO}